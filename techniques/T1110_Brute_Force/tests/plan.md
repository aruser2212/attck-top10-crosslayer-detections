# План тестирования T1110
1) Сгенерировать серию 4625 по одному пользователю с одного IP (brute) — проверить hunt win_4625_brute_agg.
2) Сгенерировать spray: по 1-2 попытки для 20 пользователей с одного IP — проверить hunt win_4625_spray_agg.
3) Для Linux — "Failed password" ≥8 раз за 5 минут по одному user×IP — проверить linux_ssh_failed_agg.
4) Для сети — генерировать частые SSH-коннекты (лабораторно) — Suricata правило должно сработать.
