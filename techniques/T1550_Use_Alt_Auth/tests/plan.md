# План тестирования: T1550 (PtH / PtT)

Цели:
- Поймать PtH: LT=9 (NewCredentials) + seclogo; усилить корреляцией с 4672.
- Поймать PtT: всплеск 4769 без 4768 в окне (эвристика).

Шаги (лабораторно/описательно):
1) **PtH**: выполните `runas /netonly /user:DOM\user cmd` на тестовой машине → ожидаем 4624 (LT=9, seclogo); проверьте Hunt first-seen и корреляцию с 4672.
2) **PtT**: сгенерируйте серию запросов TGS (4769) без AS-REQ в том же окне (имитация/реплей синтетики) → ожидаем срабатывание hunt.
3) Зафиксируйте легит-исключения (админские сценарии, app-серверы) и добавьте в allow-lists DetCards.